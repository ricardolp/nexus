# =================================================================
# Backend API
# =================================================================

# Server-side (runtime). Use this in deploy (Railway, PM2, Docker).
# Example: API_URL=https://seu-backend.railway.app
API_URL=http://localhost:4000

# Cookie Secure flag. Auto-detects HTTPS when unset.
# Set to "false" for HTTP deploys (ex.: http://IP:3000 na Azure sem SSL).
# AUTH_COOKIE_SECURE=false

# Client-side (build-time). Only needed for direct browser calls to the backend.
# The BFF (/api/backend/*) uses API_URL above and does not require this in deploy.
NEXT_PUBLIC_API_URL=http://localhost:4000

# =================================================================
# Error Tracking Configuration (Sentry)
# =================================================================

NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
NEXT_PUBLIC_SENTRY_DISABLED=true

# =================================================================
# Build Configuration
# =================================================================

BUILD_STANDALONE=
